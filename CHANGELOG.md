# 척추의 요정 — 2024 프로토타입 개선 기록

2024년 6월 캡스톤 프로젝트로 만든 웹 프로토타입의 문제를 정리하고 수정한 내역.
Teachable Machine Pose 모델은 원본을 그대로 쓰고, 로직을 고친 뒤 디자인을 다시 잡았다.

## 수정 내역

### 1. 총 측정 시간 계산 오류
**증상** 총 시간이 실제 경과 시간보다 수십 배 크게 표시됨.

**원인** `totalTime += (Date.now() - statusStartTime) / 1000` 이 매 프레임 실행되면서
델타가 아니라 "상태 시작 이후 누적 시간 전체"를 매번 더하고 있었다.
초당 60프레임이므로 10초 경과 시점에는 초당 600초씩 증가한다.

**수정** 직전 틱과의 차이만 누적. 절전·탭 전환으로 생기는 긴 공백은
`MAX_DT = 5초`로 잘라 비정상 값이 들어가지 않게 했다.

### 2. 탭이 숨겨지면 측정이 중단됨
**증상** 다른 창에서 작업을 시작하면 측정이 조용히 멈춤. 자세 교정 앱인데
해당 탭을 보고 있어야만 동작한다는 모순.

**원인** `requestAnimationFrame` 루프. 브라우저는 탭이 비활성화되면 이 콜백을 호출하지 않는다.

**수정** `setInterval(tick, 250)`으로 교체. 숨김 상태에서는 브라우저가 초당 1회로
제한하지만 자세 측정에는 충분하다. 더불어 추론 빈도가 초당 60회에서 4회로 줄어
CPU 사용량이 크게 감소했다.

> 다만 이는 완화일 뿐 해결은 아니다. 브라우저 탭 자체를 닫으면 여전히 측정이 끝난다.
> 트레이 상주 데스크톱 앱으로 옮겨야 하는 근거.

### 3. 알림음이 연속으로 터짐
**증상** 경계 자세에서 알림이 반복 재생되어 사용이 불가능한 수준.

**원인** 단일 임계값(확률 0.9)으로 판정하고, 상태가 바뀌는 즉시 소리를 재생했다.
확률이 0.89와 0.91 사이를 오가면 판정이 매 프레임 뒤집힌다.

**수정**
- 히스테리시스 적용: 진입 0.75 / 해제 0.55로 기준 분리
- 20초 이상 지속될 때만 알림 (자료를 보려고 잠깐 숙이는 동작은 무시)
- 알림 간 최소 간격 5분
- 한 에피소드당 알림 1회

### 4. 알림음 파일 누락
**원인** `new Audio('101_바운스1.mp3')` 가 참조하는 파일이 프로젝트에 없어 재생 실패.

**수정** 외부 파일 의존을 없애고 Web Audio API로 짧은 알림음을 생성.
브라우저 알림(Notification API)도 추가해 탭이 가려져 있어도 알림이 보이게 했다.

### 5. 측정 데이터가 저장되지 않음
**증상** 새로고침하면 기록이 사라짐. 통계 페이지가 감지 페이지와 연결되지 않음.

**원인** 기록이 메모리 배열에만 존재했고, `statics.html` 의 요일별 데이터는
`[30, 45, 60, 20, 50, 30, 10]` 으로 하드코딩되어 있었다.

**수정** `store.js` 추가. localStorage에 날짜별로 다음을 저장한다.
- 총 측정 시간, 거북목 시간
- 최장 연속 거북목 시간
- 알림 횟수
- 분 단위 버킷 `minutes` (0~1439), 측정한 분만 담는 희소 구조

집계값만으로는 "언제" 거북목이었는지를 복원할 수 없어 분 단위로 저장한다.
5초 주기와 페이지 이탈 시점에 저장하며 최근 60일치를 유지한다.

### 6. statusDurations NaN
**원인** `statusDurations` 객체에 `'거북목'` 키만 초기화되어 있어
다른 상태에서는 `undefined + duration` 이 되어 NaN이 저장됐다.

**수정** 상태를 `GOOD / BAD / UNKNOWN` 열거형으로 바꾸고 누적 변수를 명시적으로 초기화.

### 7. 기록을 읽을 수 없었음
**증상** 측정 화면에 "오후 7:45:59 부터 22초 동안 거북목" 같은 텍스트 목록이
실시간으로 쌓였다. 한눈에 들어오지 않고, 측정 중에 들여다볼 일도 없었다.

**수정** 텍스트 목록을 걷어내고 통계 페이지에 타임라인 그래프를 넣었다.
애플 건강 앱의 수면 그래프와 같은 형태로, 가로가 시간축이고 위 줄이 거북목,
아래 줄이 바른 자세다. 측정되지 않은 구간은 빈칸으로 남는다.
아래 수치는 시간과 비율을 토글로 전환한다.

**측정 화면에서는 그래프를 모두 제거했다.** 도넛 차트를 초당 60회 다시 그리던
것도 함께 사라져 상시 실행 부담이 줄었다.

### 7-1. 알림 주기를 사용자가 정할 수 있게
지속 시간(10초~1분)과 반복 주기(3~15분)를 선택하도록 했다. 적정값은 사람마다
다르고, 직접 써보기 전에는 알 수 없다.

### 8. 디자인 정리
**문제** 헤더와 네비게이션 CSS가 네 파일에 복붙되어 한 곳을 고치려면 네 군데를
고쳐야 했다. 페이지마다 주색도 달랐다 (측정 파랑 `#1e88e5`, 통계 보라 `#6200ea`).
고정 너비 때문에 창을 줄이면 헤더가 깨졌고, 폰트는 Medium 한 종류만 불러와
제목과 본문의 위계가 없었다.

**수정**
- `style.css` 하나로 통합, 색은 전부 CSS 변수로 분리
- 아이보리 배경 + 딥 포레스트 그린 팔레트로 통일
- 테라코타(`#C4703A`)는 거북목 상태에만 사용. 장식으로 쓰지 않아 색만 보고
  그래프를 읽을 수 있다
- Pretendard로 교체. 400 / 500 / 700 세 굵기로 위계를 만들고, 본문 글줄을
  58ch로 제한해 문장이 어색한 위치에서 끊기지 않게 했다
- 760px 이하 반응형, 키보드 포커스 표시, 모션 최소화 설정 반영
- `소개` 페이지 추가 (인터뷰, 알림 효과 비교, 시장 분석, 향후 계획)

### 9. 기타
- `sh.html` 이 `posture.html` 과 바이트 단위로 동일한 중복 파일 → 삭제
- 사람이 감지되지 않을 때(`UNKNOWN`)는 시간을 누적하지 않도록 처리
- 자정을 넘기면 자동으로 날짜를 넘겨 저장
- 일시정지 버튼 추가
- 웹캠 테두리와 상태 배지 색으로 현재 상태 표시
- `home.html` → `index.html` (GitHub Pages 기본 문서)

## 남은 한계

수정으로 해결되지 않는, 구조를 바꿔야 하는 문제들.

1. **개인 캘리브레이션 부재** — Teachable Machine 모델은 학습시킨 사람의 체형,
   카메라 각도, 앉은 거리에 맞춰져 있다. 다른 사용자나 다른 환경에서는 정확도가 떨어진다.
2. **브라우저 의존** — 탭을 닫으면 끝난다. 자동 실행도, 트레이 상태 표시도 불가능.
3. **모델 외부 호스팅** — `teachablemachine.withgoogle.com` 에 의존하므로 오프라인 사용 불가.
4. **정확도 검증 부재** — 실제 거북목 여부(측면 CVA 측정)와 대조한 적이 없어
   판정이 얼마나 맞는지 알 수 없다.

다음 버전에서는 정면 랜드마크의 기하학적 특징값 + 사용자별 캘리브레이션으로 판정 방식을 바꾸고,
Electron 트레이 앱으로 옮겨 1, 2, 3번을 해결한다.
