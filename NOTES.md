# 척추의 요정 — 작업 인수인계

Claude Code로 이 폴더를 열었다면 이 문서를 먼저 읽으면 된다.
지금까지 내린 결정과 그 이유, 남은 과제를 정리해뒀다.

---

## 1. 프로젝트가 뭔지

거북목(전방두부자세) 탐지 및 교정 서비스. 웹캠으로 앉은 자세를 보고,
목이 앞으로 나온 상태가 일정 시간 이어지면 알려주고, 하루치 기록을 통계로 보여준다.

- **원본**: 2024년 3~6월 국민대학교 경영정보학부 캡스톤디자인. 동작하는 웹 프로토타입까지 만들었으나 실사용에는 못 미쳤음
- **현재**: 2026년에 그 프로토타입을 다시 손보는 중. 포트폴리오 및 자기소개서용
- **서술 방식**: "기획 2024 / 구현·개선 2026"으로 시점을 나눠서 명시한다. 2024년 기술로 굳이 맞출 이유는 없다

---

## 2. 파일 구조

```
index.html     랜딩. 히어로 + 기능 소개 + CTA (GitHub Pages 기본 문서)
about.html     소개. 인터뷰, 알림 효과 비교, 시장 분석, 향후 계획
posture.html   측정. 웹캠 + 판정 + 알림. 그래프 없음
statics.html   통계. 타임라인 + 주간 추이 + 일별 기록
reels.html     스트레칭 영상 링크
style.css      공통 디자인 토큰 및 컴포넌트
store.js       localStorage 저장소 + formatTime 유틸
timeline.js    캔버스 타임라인 렌더러
images/        로고, 히어로 이미지, 릴스 썸네일
```

**의존 관계 주의**: `posture.html`과 `statics.html`은 `store.js`가 없으면
스크립트 맨 윗줄에서 죽는다. 이때 화면에는 엉뚱하게 "모델을 불러오지 못했습니다"가
뜬다. (실제로 이 문제로 한참 헤맸음.) 파일을 옮길 때는 항상 세트로 옮긴다.

---

## 3. 기술 결정과 이유

### 구현 형태: 웹 (서버 없음)
브라우저 안에서만 동작한다. 영상은 기기 밖으로 나가지 않는다.
프라이버시가 이 서비스의 핵심 설득 포인트라서 의도적으로 서버를 두지 않았다.

### 탐지: Teachable Machine Pose
- 모델 URL: `https://teachablemachine.withgoogle.com/models/XKictqi54/`
- PoseNet으로 관절 좌표 17개를 뽑고 그 패턴을 분류한다. 픽셀을 직접 보는 게
  아니라 조명이나 옷 색에 덜 흔들린다
- 클래스 이름에 '거북'이 들어간 것을 나쁜 자세로 본다 (`isBadLabel`)
- **위험 요소**: Google 서버 호스팅이라 모델이 삭제되면 복구 불가.
  Teachable Machine에서 TensorFlow.js로 내보내 폴더에 넣어두는 게 안전하다.
  단 로컬 모델은 `file://`에서 안 되고 로컬 서버가 필요하다

### 정면 촬영을 쓰는 이유
측면(귀-어깨 정렬)이 임상적으로 정확하지만, 책상 옆에 카메라를 둘 수 없는 게
현실이라 정면으로 간다. 정면에서도 판별이 되는 근거는 이렇다.

머리만 앞으로 나오면 원근 때문에 **얼굴은 커지는데 어깨 너비는 그대로**다.
몸 전체가 앞으로 간 경우에는 둘 다 같이 커진다. 이 차이가 판별의 핵심이다.
어깨 너비로 나눈 비율값을 쓰면 앉은 거리가 달라져도 안정적이다.

### 판정
```
SAMPLE_MS = 250    초당 4회 추론 (원본은 매 프레임 = 초당 60회)
ENTER_BAD = 0.75   이 확률을 넘으면 거북목 진입
EXIT_BAD  = 0.55   이 확률 밑으로 내려와야 해제
MAX_DT    = 5초    절전·탭전환 공백 방어
```
진입과 해제 기준을 나눈 것(히스테리시스)은 경계 확률에서 판정이 매 프레임
뒤집히면서 알림이 연속으로 터지는 걸 막기 위해서다.

`UNKNOWN`(사람 미감지) 상태에서는 시간을 누적하지 않는다. 자리를 비운 시간이
분모에 들어가면 비율이 왜곡된다.

### 알림
- 거북목이 **설정한 지속 시간**(기본 20초) 이상 이어지면 알림
- 고쳐지지 않으면 **설정한 주기**(기본 5분)로 반복
- 자세를 폈다 다시 무너져도 마지막 알림 후 주기가 안 지났으면 안 울림
- 지속 시간(10초/20초/30초/1분)과 주기(3/5/8/10/15분) 모두 사용자가 선택
- 알림 시 4가지가 동시에: 비프음(Web Audio), 페이지 내 오버레이,
  브라우저 알림(`tag: 'posture'`로 중복 방지), 탭 제목 깜빡임
- 자세를 펴면 오버레이와 제목 깜빡임이 자동으로 꺼진다

### 측정 루프
`requestAnimationFrame`이 아니라 `setInterval`을 쓴다.
rAF는 탭이 숨겨지면 멈추기 때문에, 다른 창에서 일을 시작하는 순간 측정이
조용히 중단된다. 자세 교정 서비스인데 그 탭을 봐야 한다는 건 모순이다.
`setInterval`은 숨김 시 초당 1회로 제한되지만 자세 측정에는 충분하다.

### 저장
`localStorage` 키 `spine-fairy-v2`. 날짜별로 다음을 저장한다.
```js
{ total, bad, longestBad, alerts, minutes: { "1065": { b: 42.3, t: 60 } } }
```
`minutes`는 분 단위 희소 버킷(0~1439). 타임라인 그래프가 이걸 쓴다.
집계값만으로는 "언제" 정보를 복원할 수 없어서 이렇게 바꿨다.
최근 60일 유지, 5초 주기와 페이지 이탈 시 저장.

설정은 별도 키 `spine-fairy-settings`.

### 통계
**측정 화면에는 그래프를 두지 않는다.** 측정 중에 통계를 들여다볼 일이 없고,
1초마다 캔버스를 다시 그리는 건 낭비다. 통계 페이지에서 한 번만 그린다.

타임라인은 애플 건강 앱의 수면 그래프를 참고했다. 위 줄이 거북목, 아래 줄이
바른 자세, 가로가 시간축. 측정 안 된 구간은 빈칸으로 남는다.
아래 수치는 시간/비율 토글로 전환한다.

---

## 4. 디자인 시스템

```css
--bg: #F4F3EC        배경 아이보리
--surface: #FFFFFF   카드
--ink: #24301F       본문
--ink-soft: #6B7A67  보조 텍스트
--line: #E2E0D5      구분선
--primary: #2E4B32   딥 포레스트 (버튼, 현재 네비)
--sage: #DCE6D5      연한 세이지 (배경 강조)
--good: #6A9A5B      바른 자세
--bad: #C4703A       거북목
```

**테라코타(`--bad`)는 거북목 상태에만 쓴다. 장식으로 쓰지 않는다.**
색이 의미를 전달해야 타임라인이든 막대 그래프든 색만 보고 읽힌다.

- 폰트: Pretendard (jsDelivr CDN, 다이나믹 서브셋). 본문 400, 강조 500, 제목 700.
  SIL 오픈 폰트 라이선스라 저장소에 파일을 넣지 않아도 되고 라이선스 문제도 없다
- 본문 글줄은 `max-width: 58ch`. 문장이 어색한 위치에서 끊기지 않게
- 카드는 흰 배경 + 1px 테두리, 그림자 없음
- 네비게이션 현재 페이지는 알약형 채움
- 760px 이하에서 네비가 가로 스크롤, 카드가 한 줄로

---

## 5. 2024년 원본에서 고친 것

포트폴리오에 쓸 내용이라 원인까지 적어둔다. 자세한 건 `CHANGELOG.md` 참고.

1. **총 시간 폭주** — 매 프레임 "상태 시작 이후 전체 시간"을 더하고 있었다.
   델타만 더하도록 수정
2. **탭 숨기면 측정 중단** — rAF → setInterval
3. **알림 연속 발생** — 단일 임계값 + 즉시 재생 → 히스테리시스 + 지속시간 + 주기
4. **알림음 파일 누락** — 참조하던 mp3가 없었음. Web Audio로 대체
5. **데이터 미저장** — 통계 페이지가 `[30,45,60,20,50,30,10]` 하드코딩이었음.
   localStorage 연결
6. **statusDurations NaN** — `'거북목'` 키만 초기화되어 다른 상태에서 NaN
7. **도넛 차트에 시리즈 1개** — 항상 꽉 찬 원. 2개로 수정 후 결국 타임라인으로 교체
8. **차트 초당 60회 재렌더** — 1초 주기로 낮춘 뒤 측정 화면에서 제거
9. **`sh.html`이 `posture.html`과 바이트 단위 동일** — 삭제

---

## 6. 남은 과제

우선순위 순.

1. **개인 캘리브레이션 부재** (가장 큰 한계)
   지금 모델은 학습시킨 사람의 체형, 카메라 각도, 앉은 거리 기준이다.
   다른 사용자나 노트북 각도가 바뀌면 정확도가 떨어진다.
   - 간이안: 페이지에서 "바른 자세 5초 / 거북목 5초"를 찍어 개인 임계값 산출
   - 정밀안: 폰으로 측면을 동시 촬영해 CVA(귀구슬–C7 각도, 50도 미만이 거북목)로
     라벨을 만들고, 정면 특징값 4~5개짜리 로지스틱 회귀를 사용자별로 학습
   - 노트북은 세션마다 화면 각도가 바뀌므로 시작 시 3초 재기준화가 필요하다

2. **`about.html` 내용 교체**
   지금 들어 있는 인터뷰 인용과 알림 효과 수치는 임시 텍스트다.
   2024년 인터뷰·시장조사 원본으로 바꿔야 한다. 점선 박스로 표시해둠

3. **정확도 검증 부재**
   실제 거북목 여부와 대조한 적이 없어 판정이 얼마나 맞는지 모른다

4. **기록 내보내기/불러오기**
   브라우저 데이터를 지우면 기록이 날아간다. JSON 내보내기 버튼이면 충분

5. **모델 로컬화**
   Google 서버 의존 제거

6. **홈 히어로 이미지**
   지금은 기존 `image1~3.png`를 넣어뒀다. 마스코트 캐릭터가 있다면 교체

**의도적으로 안 하기로 한 것**: 측정 화면의 실시간 그래프, 서버/DB 연동,
과도한 에러 진단 코드(한 번 넣었다가 걷어냄).

---

## 7. 나중에 검토할 것

브라우저 버전의 한계는 결국 이렇다. 탭을 닫으면 끝나고, 부팅할 때마다 직접
열어야 하고, 화상회의와 카메라가 충돌한다. 이걸 해결하려면 Electron 트레이
상주 앱으로 가야 한다. 기존 HTML을 거의 그대로 창 안에 넣을 수 있다.

다만 **지금은 웹 버전을 완성하는 데 집중한다.** 트레이 앱은 별도 판단이 설 때까지
보류.

---

## 8. 실행 방법

```bash
python3 -m http.server 8000
# http://localhost:8000
```

`file://`로 열어도 대체로 동작하지만 로컬 서버 쪽이 확실하다.
웹캠 권한과 브라우저 알림 권한이 필요하다.
